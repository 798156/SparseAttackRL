# 🔬 Week 2 Day 6 - 参数敏感性分析

## 🎯 今天的目标

**主要任务：** 分析不同参数对攻击性能的影响

**预计时间：** 2-3小时

**为什么重要：**
- ✅ 展示对方法的深入理解
- ✅ 找出最优参数配置
- ✅ 评估方法的稳定性
- ✅ 为实际应用提供指导
- ✅ 增加论文工作量感知

---

## 📋 实验设计

### **测试方法：**
5个攻击方法：
- JSMA
- SparseFool
- Greedy
- PixelGrad
- RandomSparse

### **测试参数：**
```python
JSMA:          max_pixels = [5, 10, 15, 20]
SparseFool:    max_iter = [10, 20, 30, 50]
Greedy:        max_pixels = [5, 10, 15, 20]
PixelGrad:     max_pixels = [5, 10, 15, 20]
RandomSparse:  max_pixels = [5, 10, 15, 20]
```

### **实验规模：**
- **测试模型：** ResNet18（代表性模型）
- **样本数：** 每个配置50个样本
- **总测试数：** 5方法 × 4参数 × 50样本 = **1,000个测试**
- **预计时间：** 2-3小时

---

## 🚀 快速开始

### **步骤1：确认环境**
```bash
# 确认在虚拟环境中
# (.venv) 应该在命令行提示符前显示

# 确认模型存在
ls cifar10_resnet18.pth
```

### **步骤2：运行脚本**
```bash
python analyze_parameter_sensitivity.py
```

### **运行过程中你会看到：**
```
🚀🚀🚀... Starting Parameter Sensitivity Analysis ...

Loading model: resnet18
Loading test data...
Selecting 50 correctly classified samples...
✓ Selected 50 samples

#### Method: JSMA
#### Parameter: max_pixels
#### Values: [5, 10, 15, 20]

============================================================
Testing JSMA: max_pixels=5
============================================================
JSMA: 100%|████████████████| 50/50 [00:30<00:00]
📊 Results: ASR=75.0%, L0=4.5, Time=0.450s

... (继续测试其他参数值) ...
```

---

## 📊 预期产出

### **1. 数据文件（JSON）**
```
results/parameter_sensitivity/
├── jsma_sensitivity.json
├── sparsefool_sensitivity.json
├── greedy_sensitivity.json
├── pixelgrad_sensitivity.json
└── randomsparse_sensitivity.json
```

### **2. 可视化图表（PNG + PDF）**
```
results/parameter_sensitivity/
├── asr_sensitivity_curves.png/pdf     # ASR曲线
├── l0_sensitivity_curves.png/pdf      # L0曲线
├── time_sensitivity_curves.png/pdf    # 时间曲线
└── comprehensive_heatmap.png/pdf      # 综合热图
```

### **3. 分析报告（Markdown）**
```
results/parameter_sensitivity/
└── sensitivity_analysis_report.md     # 完整报告
```

---

## 📈 结果解读

### **1. 最优参数**
报告会告诉你每个方法的最优参数配置：
```
JSMA: max_pixels=15 (ASR=85.0%)
SparseFool: max_iter=30 (ASR=60.0%)
Greedy: max_pixels=10 (ASR=80.0%)
...
```

### **2. 稳定性排名**
哪个方法对参数变化最不敏感（最稳定）：
```
1. Greedy - Std: 5.2%
2. JSMA - Std: 8.1%
3. SparseFool - Std: 12.3%
...
```

**解释：**
- Std（标准差）越小 = 越稳定
- 稳定的方法 = 参数选择更容易

### **3. 权衡分析**
- **高ASR但慢：** max_pixels=20时ASR高，但时间长
- **速度快但ASR低：** max_pixels=5时快，但ASR低
- **最佳平衡点：** 通常在max_pixels=10或15

---

## 🎨 图表说明

### **图1：ASR敏感性曲线**
- X轴：参数值（如max_pixels）
- Y轴：攻击成功率（%）
- 多条曲线：不同方法

**看什么：**
- 曲线越平 = 越稳定
- 峰值在哪 = 最优参数

### **图2：L0敏感性曲线**
- 显示参数如何影响修改像素数
- 理想情况：ASR高 + L0低

### **图3：时间敏感性曲线**
- 显示参数如何影响计算时间
- 帮助选择效率-效果平衡点

### **图4：综合热图**
- 一次性查看所有指标
- 颜色深浅表示数值大小

---

## ✅ 完成检查清单

运行结束后，确认：
- [ ] 所有5个方法都测试完成
- [ ] 生成了4张图表（PNG + PDF）
- [ ] 生成了分析报告
- [ ] 控制台打印了摘要信息

---

## 💡 常见问题

### **Q1: 运行时间太长怎么办？**
```python
# 在脚本中修改（第31行）：
'samples_per_config': 30,  # 从50减少到30
```

### **Q2: 想测试更多参数值？**
```python
# 修改PARAMETER_CONFIGS（第40-65行）：
'JSMA': {
    'param_values': [3, 5, 10, 15, 20, 25],  # 增加更多值
    ...
}
```

### **Q3: 只想测试某几个方法？**
```python
# 注释掉不想测试的方法（第40-65行）：
PARAMETER_CONFIGS = {
    'JSMA': {...},
    'Greedy': {...},
    # 'SparseFool': {...},  # 注释掉
    # 'PixelGrad': {...},
    # 'RandomSparse': {...},
}
```

### **Q4: 内存不够？**
```python
# 减少样本数或分批运行
# 先测试3个方法，再测试另外2个
```

---

## 🎯 结果用于论文

### **可以写的内容：**

**1. 实验设置部分：**
```
To identify optimal parameters, we conducted sensitivity 
analysis on ResNet18 with 50 samples per configuration.
We tested max_pixels ∈ {5, 10, 15, 20} for pixel-based 
methods and max_iter ∈ {10, 20, 30, 50} for SparseFool.
```

**2. 结果分析部分：**
```
Figure X shows the parameter sensitivity curves. 
We observe that Greedy is most stable (Std=5.2%), 
while RandomSparse shows high sensitivity (Std=18.6%).
The optimal max_pixels is 15 for JSMA (ASR=85%), 
balancing effectiveness and efficiency.
```

**3. 讨论部分：**
```
The stability analysis reveals that gradient-based 
methods (JSMA, Greedy) are less sensitive to parameter 
changes compared to random methods, suggesting more 
robust pixel selection strategies.
```

### **可以插入的图表：**
- 图1：ASR敏感性曲线（主要结果）
- 图2或图4：作为补充材料

---

## 📝 运行后的下一步

### **立即可做：**
1. ✅ 查看生成的图表（用PDF查看器打开）
2. ✅ 阅读分析报告（sensitivity_analysis_report.md）
3. ✅ 记录关键发现（为论文做笔记）

### **明天继续：**
- Week 2 Day 8-9: 失败案例分析
- 目标：找出哪些样本难以攻击，为什么

---

## 🎉 成功标准

完成后你应该有：
- ✅ 1,000个新的测试数据点
- ✅ 4张高质量图表
- ✅ 完整的敏感性分析报告
- ✅ 每个方法的最优参数
- ✅ 方法稳定性排名

**这些都是论文的宝贵素材！** 💎

---

**现在运行脚本，2-3小时后见！** 🚀

```bash
python analyze_parameter_sensitivity.py
```

**遇到问题随时告诉我！** 💪















